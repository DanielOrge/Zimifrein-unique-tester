// 1. Unused variable (warning)
const unusedVar = 42;

// 2. Console statement (warning)
console.log('This is a warning-level issue.');

// 3. Function with an implicit return type (warning)
function add(a: number, b: number) {
  return a + b;
} 